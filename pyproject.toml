[project]
name = "patience"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.1",
    "django-environ>=0.12.0",
    "pre-commit>=4.2.0",
    "psycopg[binary]>=3.2.7",
    "ruff>=0.11.8",
]
